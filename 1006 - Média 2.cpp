#include<bits/stdc++.h>
using namespace std;

int main() {	
	ios_base::sync_with_stdio(false), cin.tie(0), cout.tie(0);
	
	float a, b, c; cin >> a >> b >> c;
	
	printf("MEDIA = %.1f\n", (a * 2 + b * 3 + c * 5) / (2 + 3 + 5));
	
	return 0;
}
